# Optional dependencies for ONNX QCDQ export of LLMs (brevitas_examples/llm).
# `optimum` (via `optimum-onnx`) provides `onnx_export_from_model`. We install the
# highest available version; note that `optimum-onnx` currently constrains
# `transformers` to < 4.58, so this extra is not compatible with transformers >= 5.0.
optimum[onnxruntime]
